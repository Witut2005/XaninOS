
#include <stdint.h>

namespace std
{

/*
template<class input_type, class f_type>
extern f_type for_each(input_type begin, input_type end, f_type function );

template<class T>
extern constexpr T& max(T& x, T& y);

template<class T>
extern constexpr T& min(T& x, T& y);

template<class T, class X>
extern constexpr bool equal(const T*  beg, const T*  end, const X*  beg2);

template<class T> 
extern constexpr void swap(T& x, T& y);

template <class T>
extern T* find(const T* beg, const T* end, const T value);
*/

}
