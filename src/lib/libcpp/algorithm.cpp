
#include <stdint.h>
#include <lib/libc/stdlibx.h>
#include <lib/libcpp/utility.h>

// extern "C" int** matrix90rotate32(int** matrix, uint32_t x, uint32_t y)
// {
//     int** tmp = (int**)calloc(SIZE_OF(int*) * x);

//     for(int i = 0; i < x; i++)
//     tmp[i] = (int*)calloc(SIZE_OF(int) * y);
     
//     for(int i = 0; i < ; i++)
//     {                                    
//         for(std::pair<int, int> para(0, 3); para.first < 4; para.first++, para.second--)
//             tmp[i][para.first] = matrix[para.second][i];
//     }

//     return tmp;

// }